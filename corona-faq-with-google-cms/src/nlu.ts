import { Intent } from "narratory"

export const yes: Intent = {
  examples: ["yes", "absolutely", "of course", "yep", "okay", "alright"]
}

export const no: Intent = {
  examples: ["no", "nope", "never", "I don't", "none", "nothing"]
}